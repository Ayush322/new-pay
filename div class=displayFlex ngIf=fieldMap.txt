checkTaxType(index: number) {
    if (this.beneficiaryArray.at(index).get('taxIdType').value !== BankerOpenAccountConstants.NOTAXID) {
      this.beneficiaryArray.at(index).get('beneficiaryTaxId').enable();
      return true;
    } else {
      this.beneficiaryArray.at(index).get('beneficiaryTaxId').disable();
      return false;
    }
  }

<div class="displayFlex" *ngIf="fieldMap?.beneficiaryRel?.isVisible">
                        <mat-form-field>
                            <mat-label><span message-key="OAO_NEWRETIREMENT_RELATIONSHIP_LBL"></span></mat-label>
                            <mat-select
                                formControlName="beneficiaryRel">
                                <mat-option value=""><span
                                        message-key="OAO_NEWRETIREMENT_PLANTYPE_SELECTOPT_LBL"></span></mat-option>
                                <mat-option *ngFor="let obj of relationships" [value]="obj.name">
                                    {{ obj.value }}
                                </mat-option>
                            </mat-select>
                            <mat-error *ngIf="form.invalid">
                                {{geBeneficiaryError('beneficiaryRel',i)}}</mat-error>
                        </mat-form-field>
                    </div>


};
  bindMiscData(category, data) {
    switch (category) {
       
        case CodeCategoryConstants.IRA_BENEFICIARY_TYPE:
            this.relationships = data;
            break;

}

what logic should be implemented to get this 
Relationship is the field andthe valueinside it is below

Spouse
Non-Spouse
Entity/Trust
Estate

If Estate value is selected, the TIN information is not required. 
Add this value to the current list.